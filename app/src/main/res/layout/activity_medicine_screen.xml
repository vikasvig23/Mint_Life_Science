<?xml version="1.0" encoding="utf-8"?>
<layout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools">

    <data />

    <androidx.constraintlayout.widget.ConstraintLayout
        android:id="@+id/main"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        tools:context=".medicinePresentation.MedicineScreenActivity">

        <!-- ══════════ TOOLBAR ══════════ -->
        <androidx.appcompat.widget.Toolbar
            android:id="@+id/toolbar"
            android:layout_width="match_parent"
            android:layout_height="?attr/actionBarSize"
            android:background="?attr/colorPrimary"
            app:layout_constraintTop_toTopOf="parent"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintEnd_toEndOf="parent">

            <ImageView
                android:id="@+id/back_arrow"
                android:layout_width="wrap_content"
                android:layout_height="match_parent"
                android:layout_gravity="start"
                android:padding="8dp"
                android:src="@drawable/baseline_arrow_back_24"
                android:contentDescription="Back" />

            <TextView
                android:id="@+id/toolbar_title"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:layout_gravity="center"
                android:text="Presentation"
                android:textColor="@android:color/white"
                android:textSize="20sp"
                android:textStyle="bold" />
        </androidx.appcompat.widget.Toolbar>

        <!-- ══════════ VIEWPAGER2 ══════════ -->
        <androidx.viewpager2.widget.ViewPager2
            android:id="@+id/viewPager"
            android:layout_width="0dp"
            android:layout_height="0dp"
            app:layout_constraintTop_toBottomOf="@id/toolbar"
            app:layout_constraintBottom_toTopOf="@id/submit_presentation"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintEnd_toEndOf="parent" />

        <!-- ══════════ ACTION BUTTONS (slide in from right) ══════════ -->
        <LinearLayout
            android:id="@+id/actionButtonsPanel"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:orientation="vertical"
            android:gravity="end"
            android:layout_marginEnd="14dp"
            android:layout_marginBottom="14dp"
            android:clipChildren="false"
            app:layout_constraintEnd_toEndOf="parent"
            app:layout_constraintBottom_toTopOf="@id/submit_presentation">

            <!-- Edit Feedback — violet -->
            <com.google.android.material.button.MaterialButton
                android:id="@+id/btn_edit_feedback"
                style="@style/Widget.Material3.Button"
                android:layout_width="wrap_content"
                android:layout_height="44dp"
                android:layout_marginBottom="10dp"
                android:text="Edit Feedback"
                android:textColor="@android:color/white"
                android:textSize="13sp"
                android:paddingHorizontal="16dp"
                app:icon="@drawable/baseline_edit_24"
                app:iconTint="@android:color/white"
                app:iconPadding="6dp"
                app:iconSize="18dp"
                app:cornerRadius="12dp"
                app:backgroundTint="#7C4DFF"
                android:elevation="4dp" />

            <!-- Share Presentation — ocean blue -->
            <com.google.android.material.button.MaterialButton
                android:id="@+id/btn_share_presentation"
                style="@style/Widget.Material3.Button"
                android:layout_width="wrap_content"
                android:layout_height="44dp"
                android:layout_marginBottom="10dp"
                android:text="Share PDF"
                android:textColor="@android:color/white"
                android:textSize="13sp"
                android:paddingHorizontal="16dp"
                app:icon="@drawable/baseline_share_24"
                app:iconTint="@android:color/white"
                app:iconPadding="6dp"
                app:iconSize="18dp"
                app:cornerRadius="12dp"
                app:backgroundTint="#0288D1"
                android:elevation="4dp" />

            <!-- Update Medicine — deep orange -->
            <com.google.android.material.button.MaterialButton
                android:id="@+id/btn_update_medicine"
                style="@style/Widget.Material3.Button"
                android:layout_width="wrap_content"
                android:layout_height="44dp"
                android:text="Update Medicines"
                android:textColor="@android:color/white"
                android:textSize="13sp"
                android:paddingHorizontal="16dp"
                app:icon="@drawable/baseline_refresh_24"
                app:iconTint="@android:color/white"
                app:iconPadding="6dp"
                app:iconSize="18dp"
                app:cornerRadius="12dp"
                app:backgroundTint="#E65100"
                android:elevation="4dp" />

        </LinearLayout>

        <!-- ══════════ SUBMIT BUTTON ══════════ -->
        <com.google.android.material.button.MaterialButton
            android:id="@+id/submit_presentation"
            style="@style/Widget.Material3.Button"
            android:layout_width="0dp"
            android:layout_height="52dp"
            android:layout_marginHorizontal="24dp"
            android:layout_marginBottom="16dp"
            android:text="Submit Presentation"
            android:textSize="15sp"
            android:textStyle="bold"
            android:letterSpacing="0.04"
            app:icon="@drawable/baseline_done_24"
            app:iconGravity="textStart"
            app:iconPadding="8dp"
            app:cornerRadius="26dp"
            app:layout_constraintBottom_toBottomOf="parent"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintEnd_toEndOf="parent" />

        <!-- ══════════ PROGRESS BAR ══════════ -->
        <ProgressBar
            android:id="@+id/progressBar"
            style="?android:attr/progressBarStyle"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:visibility="gone"
            app:layout_constraintTop_toTopOf="parent"
            app:layout_constraintBottom_toBottomOf="parent"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintEnd_toEndOf="parent" />

        <!-- ══════════ DIM OVERLAY ══════════ -->
        <View
            android:id="@+id/dim_overlay"
            android:layout_width="0dp"
            android:layout_height="0dp"
            android:background="#80000000"
            android:clickable="true"
            android:visibility="gone"
            app:layout_constraintTop_toTopOf="parent"
            app:layout_constraintBottom_toBottomOf="parent"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintEnd_toEndOf="parent" />

        <!-- ══════════ FRAGMENT CONTAINER ══════════ -->
        <FrameLayout
            android:id="@+id/fragment_container"
            android:layout_width="0dp"
            android:layout_height="0dp"
            android:visibility="gone"
            android:elevation="1dp"
            app:layout_constraintTop_toTopOf="parent"
            app:layout_constraintBottom_toBottomOf="parent"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintEnd_toEndOf="parent" />

    </androidx.constraintlayout.widget.ConstraintLayout>
</layout>
